---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: redisbindings.catalog.kubeware.dev
spec:
  group: catalog.kubeware.dev
  names:
    kind: RedisBinding
    listKind: RedisBindingList
    plural: redisbindings
    singular: redisbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RedisBinding is the Schema for the redisbindings API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RedisBindingSpec defines the desired state of RedisBinding
            properties:
              foo:
                description: Foo is an example field of RedisBinding. Edit redisbinding_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: RedisBindingStatus defines the observed state of RedisBinding
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
